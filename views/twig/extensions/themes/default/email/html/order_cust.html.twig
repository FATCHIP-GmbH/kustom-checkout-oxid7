{% extends "email/html/order_cust.html.twig" %}

{% block email_html_order_cust_paymentinfo_top %}
    {% if payment.oxuserpayments__oxpaymentsid.value is same as("kustom_checkout") %}
        <h3 class="underline">{{ translate({ ident: "PAYMENT_METHOD" }) }}</h3>
        <br>
        <br>
        <p>
            <b>{{ translate({ ident: "FCKUSTOM_AUTHPAYMENTMETHOD_"|cat(order.oxorder__fckustom_kustompaymentmethod.value) }) }} {% if basket.getPaymentCosts() %}({{ basket.getFPaymentCosts() }} {{ currency.sign }}){% endif %}</b>
        </p>
        <br>
        <br>
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}